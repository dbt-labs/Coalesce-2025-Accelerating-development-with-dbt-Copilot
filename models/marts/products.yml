models:
  - name: products
    description: List of products, one row per product.
    config:
      group: analytics_engineering
      meta:
        owner: analytics_engineering
    columns:
      - name: product_id
        description: The unique key for each product.
        data_tests:
          - not_null
          - unique
